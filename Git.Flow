Working Directory
       |
       | git add
       v
Staging Area
       |
       | git commit
       v
Local Repository
       |
       | git push
       v
GitHub (Remote Repository)

2. an example
git add .
git commit -m "Add new customization"
git push origin main
